import { Placeholder } from "../_components/placeholder";

export const dynamic = "force-dynamic";

export default function MobileMetrics() {
  return (
    <Placeholder
      title="Metrics"
      blurb="Pipeline + revenue metrics land in a later Phase 1 commit."
    />
  );
}
