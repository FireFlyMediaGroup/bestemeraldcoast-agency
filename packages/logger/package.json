{
  "name": "@bec/logger",
  "version": "0.0.0",
  "private": true,
  "description": "Pino + Sentry + Axiom transports (ADR-012).",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./src/index.ts",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc",
    "dev": "echo '(noop)'",
    "lint": "echo '(noop)'",
    "type-check": "tsc --noEmit",
    "test:unit": "vitest run",
    "test-emit": "pnpm -w turbo run build --filter=@bec/logger && node scripts/test-emit.mjs"
  },
  "dependencies": {
    "@bec/config": "workspace:*",
    "@sentry/node": "^8.51.0",
    "dotenv": "^16.4.7",
    "pino": "^9.6.0",
    "pino-pretty": "^13.0.0"
  },
  "devDependencies": {
    "@bec/config-eslint": "workspace:*",
    "@bec/config-tsconfig": "workspace:*",
    "@types/node": "^22.10.5",
    "tsx": "^4.19.2",
    "typescript": "^5.7.3",
    "vitest": "^2.1.8"
  }
}
