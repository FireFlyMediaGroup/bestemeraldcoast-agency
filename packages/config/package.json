{
  "name": "@bec/config",
  "version": "0.0.0",
  "private": true,
  "description": "Per-environment configuration via Zod (ADR-038). Schema lands in Commit 0.3.",
  "type": "module",
  "main": "./src/index.ts",
  "scripts": {
    "build": "echo '(noop)'",
    "dev": "echo '(noop)'",
    "lint": "echo '(noop)'",
    "type-check": "echo '(noop)'",
    "test:unit": "echo '(noop)'"
  },
  "devDependencies": {
    "@bec/config-eslint": "workspace:*",
    "@bec/config-tsconfig": "workspace:*"
  }
}
